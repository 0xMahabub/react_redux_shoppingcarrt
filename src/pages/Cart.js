import React from 'react';
import { AppContainer } from '../Global';

const Cart = () => {
    return (
        <AppContainer>
            <h1>This is Cart Page</h1>
        </AppContainer>
    );
};

export default Cart;