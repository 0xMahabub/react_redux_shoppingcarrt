import React from 'react';
import { AppContainer } from '../Global';

const Cart = () => {
    return (
        <AppContainer>
            <h1>Cart page</h1>
        </AppContainer>
    );
};

export default Cart;