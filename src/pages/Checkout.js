import React from 'react';
import { AppContainer } from '../Global';

const Checkout = () => {
    return (
        <AppContainer>
            <h1>Checkout page</h1>
        </AppContainer>
    );
};

export default Checkout;